from django.apps import AppConfig


class SigveConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.sigve'
    verbose_name = 'SIGVE - Panel de Administración'
